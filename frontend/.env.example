# ── Set these in Vercel Dashboard → Project Settings → Environment Variables ──

# Your Render.com backend URL (get this after deploying backend to Render)
VITE_API_URL=https://banking-fraud-backend.onrender.com/api

# WebSocket URL (same host, ws:// instead of https://)
VITE_WS_URL=wss://banking-fraud-backend.onrender.com/ws
